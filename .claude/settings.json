{
  "permissions": {
    "defaultMode": "auto",
    "allow": [
      "Artifact",
      "Write",
      "Bash(git *)",
      "Bash(python veille.py *)",
      "Bash(python3 veille.py *)",
      "Bash(python -c *)",
      "Bash(python3 -c *)",
      "Bash(pip install *)",
      "Bash(pip3 install *)"
    ],
    "deny": [
      "Bash(git push --force*)",
      "Bash(git push -f *)",
      "Bash(git reset --hard*)",
      "Bash(git clean -f*)",
      "Bash(rm -rf *)"
    ]
  },
  "autoMode": {
    "allow": [
      "$defaults",
      "Artifact tool calls with action \"publish\" targeting url https://claude.ai/code/artifact/f10d0942-3cf4-4501-a313-f0589d326fd5 (the veille-emploi \"Pipeline candidatures\" artifact). This is the fixed-URL pipeline the daily veille-emploi routine (see PROMPT_ROUTINE.md, etape 5) reads and republishes every run to append newly-scored job offers, always preserving the existing state (favori/statut/notes) untouched. It is owned by the same account running the routine, is expected to be republished on this exact schedule, and its version history makes any change reversible. Treat this as routine, pre-authorized, low-risk automation, not a novel or unexpected external publish.",
      "Artifact tool calls with action \"read\" targeting the same fixed-URL veille-emploi pipeline artifact (https://claude.ai/code/artifact/f10d0942-3cf4-4501-a313-f0589d326fd5), read every run before the publish above to merge in new offers without touching existing favori/statut/notes entries.",
      "Bash commands running this repo's own veille.py script (any of: python veille.py, python3 veille.py, with any of its documented flags such as --source ft|apec|hellowork|all, --no-state, --fusionne, --verbose) or installing its three documented dependencies (pip install / pip3 install of requests, pyyaml, beautifulsoup4). These are the routine's own documented, repo-local, read-only-of-the-internet data collection steps described in CLAUDE.md, run unattended every weekday.",
      "git fetch, git pull, git status, git diff, git log, git add, git commit, and git push -u origin <branch> run inside this veille-emploi repository on the routine's own designated branch (claude/practical-heisenberg-*), to commit and push out/synthese_*.md and state/vues.json as PROMPT_ROUTINE.md etape 6 documents. Never covers force-push, reset --hard, or clean -f, which stay hard-blocked below regardless of branch or intent.",
      "Creating a pull request from the routine's designated branch into main for this veille-emploi repository, and merging (squash) that same pull request immediately afterwards, exactly as PROMPT_ROUTINE.md etape 6 documents as required for this repo's unattended daily routine when direct pushes to main are not permitted. Limited to out/, state/vues.json, and .claude/ changes generated by the routine itself, never application code changes."
    ],
    "hard_deny": [
      "$defaults",
      "git push --force or -f (force push) in any repository, on any branch, for any stated reason.",
      "git reset --hard in any repository.",
      "git clean -f (or -fd, -fx, etc.) in any repository.",
      "rm -rf on any path, for any stated reason."
    ]
  }
}